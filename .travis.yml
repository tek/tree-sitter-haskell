language: node_js
sudo: false
node_js:
  - "node"
compiler: clang-5.0
env:
  - CC=clang++-5.0 CXX=clang++-5.0 CXXFLAGS=-std=c++14
addons:
  apt:
    sources:
      - llvm-toolchain-trusty-5.0
      - ubuntu-toolchain-r-test
    packages:
      - clang-5.0
